rootProject.name = "DateTimePicker"
include(":composeApp")
include(":kmp-date-time-picker")

pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}
